rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Workspace condiviso del team AlphaInk.
    // Solo utenti autenticati possono leggere/scrivere.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
